# Release Handoff

The worker claims the sync is complete before the queue is drained.

The retry path exits without a fallback.

The release note should name the observable condition that permits rollout.
